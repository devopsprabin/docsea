server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/docsea
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

DOCSEAPATH : ../client/src/assets/images/